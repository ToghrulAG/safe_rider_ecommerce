import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Buttons, logo, active filters, primary color
  static const Color primary = Color(0xFFFF3B30);

  // Dark theme colors
  static const Color darkBackground = Color(0xFF121212); // Background color
  static const Color darkSurface = Color(0xFF1E1E1E); // Cards, Menu
  static const Color textLight = Color(0xFFF5F5F7); // Text

  // Light theme colors
  static const Color lightBackground = Color(0xFFFFFFFF); // Background color

  static const Color lightSurface = Color(
    0xFFF2F2F7,
  ); // Cards color (light grey)
  
  static const Color textDark = Color(0xFF1C1C1E); // Bold texts

  // Second texts, inactive icons
  static const Color grey = Color(0xFF8E8E93);
}
